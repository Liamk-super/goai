# LaunchScope GitHub Pages static demo

This folder is deployed as a standalone GitHub Pages artifact by
`.github/workflows/deploy-github-pages-demo.yml`.

It is intentionally dependency-free and has no API endpoint, form submission,
database connection, model invocation, analytics script, or runtime fetch. The
content is a fixed product-flow demonstration, not a live LaunchScope run or a
prediction service.
